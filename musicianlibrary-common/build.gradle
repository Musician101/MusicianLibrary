dependencies {
    compile 'com.google.code.gson:gson:2.6.2'
}

install {
    repositories.mavenInstaller {
        pom.groupId = project.group.toString() + '.' + project.pluginName.toString().toLowerCase() + '.java'
        pom.artifactId = 'minecraft'
    }
}
